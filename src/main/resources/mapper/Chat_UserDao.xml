<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.appx.dao.Chat_UserDao">

    <insert id="insert">
        insert into chat_User(userId,content,target,type,createTime)
        values(#{userId},#{content},#{target},#{type},#{createTime})
    </insert>

</mapper>